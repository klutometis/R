(use cock setup-helper-mod)

(verify-extension-name "R")

(setup-shared-extension-module
 'R
 (extension-version "0.3.7")
 compile-options: '(-X cock
                       "`R CMD config --cppflags`"
                       "`R CMD config --ldflags`"))

(run-cock -o R.wiki R-core.scm)
